{
  "name": "@workerv2/execution-adapter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Worker V2 Execution Adapter — the infrastructure adapter that DRIVES the pure Coordinator: a tiny effect loop that resolves processors, negotiates capabilities, invokes Processor.process(), feeds results back into the Coordinator, persists journals through a replaceable interface, and publishes execution events through a replaceable sink. All side effects live here; the Coordinator stays pure and deterministic. NO processors, NO rendering, NO PDF, NO image processing, NO storage/DB/queue/network/R2 implementation.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@workerv2/contracts": "workspace:*",
    "@workerv2/utils": "workspace:*",
    "@workerv2/errors": "workspace:*",
    "@workerv2/control-plane": "workspace:*",
    "@workerv2/infra-contracts": "workspace:*",
    "@workerv2/processing": "workspace:*",
    "@workerv2/runtime": "workspace:*",
    "@workerv2/coordinator": "workspace:*"
  }
}
