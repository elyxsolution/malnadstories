{
  "name": "@workerv2/worker-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Worker V2 Production Runtime — the operational layer that turns the Worker Host composition root into a production-ready runtime: durable artifact store + durable journal store + event persistence (replacing the in-memory defaults via dependency injection), external runtime/worker configuration, runtime bootstrap + worker lifecycle (startup → running → draining → stopped) with graceful shutdown + restart recovery, observational health checks (readiness/liveness/dependency/storage/backend), structured logging, optional injectable metrics, and an integration harness. It is a pure COMPOSITION concern: it modifies no Coordinator / Processor / Manifest / rendering / export behavior and introduces no business logic — it only wires durable infrastructure and drives the host.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@workerv2/contracts": "workspace:*",
    "@workerv2/utils": "workspace:*",
    "@workerv2/errors": "workspace:*",
    "@workerv2/control-plane": "workspace:*",
    "@workerv2/infra-contracts": "workspace:*",
    "@workerv2/artifact-store": "workspace:*",
    "@workerv2/execution-adapter": "workspace:*",
    "@workerv2/coordinator": "workspace:*",
    "@workerv2/blueprint": "workspace:*",
    "@workerv2/image-backend": "workspace:*",
    "@workerv2/worker-host": "workspace:*"
  }
}
