{
  "name": "@workerv2/infra-contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Worker V2 infrastructure contracts — the abstraction layer (repositories, unit of work, transactions, storage, adapters, DTOs, mappers, validation, infra events) that connects the pure domain to future infrastructure. Contracts + DTOs + outbound mappers only; NO concrete storage/DB implementations.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@workerv2/contracts": "workspace:*",
    "@workerv2/utils": "workspace:*",
    "@workerv2/errors": "workspace:*",
    "@workerv2/control-plane": "workspace:*"
  }
}
