{
  "name": "@workerv2/processing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Worker V2 processing framework — the generic, framework-independent DECLARATIVE processing model (steps, pipelines, execution plans, context, DAG validation, retry/timeout/cancellation/failure policies, processor + capability contracts) that later rendering, PDF, image, and manufacturing pipelines execute. Pure data + pure functions; NO execution engine, NO business logic (INV-5).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@workerv2/contracts": "workspace:*",
    "@workerv2/utils": "workspace:*",
    "@workerv2/errors": "workspace:*",
    "@workerv2/control-plane": "workspace:*",
    "@workerv2/infra-contracts": "workspace:*"
  }
}
