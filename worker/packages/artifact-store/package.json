{
  "name": "@workerv2/artifact-store",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Worker V2 artifact platform — the concrete content-addressed, write-once byte ArtifactStore implementing the Phase 3 storage contracts: sha256 content addressing + verification, a replaceable blob-store backend, streaming I/O, integrity verification, an artifact registry, artifact validation, and provenance metadata. Storage-backend independent; a durable object store implements the same contracts later.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@workerv2/contracts": "workspace:*",
    "@workerv2/utils": "workspace:*",
    "@workerv2/errors": "workspace:*",
    "@workerv2/control-plane": "workspace:*",
    "@workerv2/infra-contracts": "workspace:*"
  }
}
